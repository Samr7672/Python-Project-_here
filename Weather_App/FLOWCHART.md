# Weather App - Flowchart and Process Documentation

## 1. Application Initialization Flow

```
┌─────────────────────────────────────────┐
│   Program Start                         │
│   if __name__ == "__main__"             │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│   Create QApplication(sys.argv)         │
│   (Initialize PyQt5 Application)        │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│   Create WeatherApp() Instance          │
│   (Calls __init__)                      │
└──────────────┬──────────────────────────┘
               │
               ▼
        ┌──────────────┐
        │  __init__()  │
        └──────────────┘
               │
        ┌──────┴──────┐
        │  Creates    │
        │  UI Labels, │
        │  Inputs,    │
        │  Buttons    │
        │             │
        │  Calls      │
        │  initUI()   │
        └──────┬──────┘
               │
               ▼
        ┌──────────────┐
        │  initUI()    │
        └──────────────┘
               │
    ┌──────────┼──────────┐
    │          │          │
    ▼          ▼          ▼
 Set      Create    Apply CSS
 Window    Layout   Styling
 Title     & Add    & Alignment
           Widgets
               │
               ▼
    Connect Button Click
    to get_weather() method
               │
               ▼
┌─────────────────────────────────────────┐
│   weather_app.show()                    │
│   Display GUI Window                    │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│   sys.exit(app.exec_())                 │
│   Start Event Loop (Wait for Events)    │
└─────────────────────────────────────────┘
```

## 2. Detailed UI Initialization Flow (initUI Method)

```
┌──────────────────────────────┐
│   initUI() Called            │
└──────────┬───────────────────┘
           │
           ▼
┌──────────────────────────────┐
│ 1. setWindowTitle()          │
│    Title = "Weather App "    │
└──────────┬───────────────────┘
           │
           ▼
┌──────────────────────────────┐
│ 2. Create VBox Layout        │
│    vbox = QVBoxLayout()      │
└──────────┬───────────────────┘
           │
           ▼
┌──────────────────────────────┐
│ 3. Add Widgets to Layout     │
│    ├─ city_label             │
│    ├─ city_input             │
│    ├─ get_weather_button     │
│    ├─ temperature_label      │
│    ├─ emoji_label            │
│    └─ description_label      │
└──────────┬───────────────────┘
           │
           ▼
┌──────────────────────────────┐
│ 4. Set Layout to Widget      │
│    self.setLayout(vbox)      │
└──────────┬───────────────────┘
           │
           ▼
┌──────────────────────────────┐
│ 5. Align All Widgets         │
│    All use Qt.AlignCenter    │
│    ├─ city_label             │
│    ├─ city_input             │
│    ├─ temperature_label      │
│    ├─ emoji_label            │
│    └─ description_label      │
└──────────┬───────────────────┘
           │
           ▼
┌──────────────────────────────┐
│ 6. Set ObjectNames           │
│    (For CSS Selection)       │
│    ├─ #city_label            │
│    ├─ #city_input            │
│    ├─ #get_weather_button    │
│    ├─ #temperature_label     │
│    ├─ #emoji_label           │
│    └─ #description_label     │
└──────────┬───────────────────┘
           │
           ▼
┌──────────────────────────────┐
│ 7. Apply CSS Stylesheet      │
│    ├─ Font: Calibri          │
│    ├─ city_label: 40px,      │
│    │  italic                 │
│    ├─ city_input: 40px       │
│    ├─ button: 30px, bold     │
│    ├─ temperature: 75px      │
│    ├─ emoji: 100px,          │
│    │  Segoe UI emoji         │
│    └─ description: 50px      │
└──────────┬───────────────────┘
           │
           ▼
┌──────────────────────────────┐
│ 8. Connect Button Signal     │
│    get_weather_button.clicked│
│    .connect(get_weather)     │
└──────────┬───────────────────┘
           │
           ▼
       Return from initUI()
```

## 3. Main Event Flow - User Interaction

```
┌──────────────────────────────────────┐
│   Application Running                │
│   Event Loop Active                  │
└──────────┬───────────────────────────┘
           │
           ▼
┌──────────────────────────────────────┐
│   User Enters City Name              │
│   Type in QLineEdit (city_input)     │
└──────────┬───────────────────────────┘
           │
           ▼
┌──────────────────────────────────────┐
│   User Clicks Get Weather Button     │
└──────────┬───────────────────────────┘
           │
           ▼
    ┌──────────────────┐
    │   get_weather()  │
    │   Method Called  │
    └──────────────────┘
           │
    ┌──────┴──────────────────┐
    │                         │
    ▼                         ▼
┌─────────────────┐    ┌──────────────────┐
│ Extract City    │    │ Create API URL   │
│ Name from       │    │                  │
│ city_input      │    │ Base URL:        │
│ Field           │    │ openweathermap   │
│                 │    │ .org/data/2.5/   │
│ city =          │    │ weather          │
│ self.city_input │    │                  │
│ .text()         │    │ Parameters:      │
│                 │    │ - q={city}       │
│                 │    │ - appid={key}    │
│                 │    │ - units=metric   │
└────────┬────────┘    └────────┬─────────┘
         │                      │
         └──────────┬───────────┘
                    │
                    ▼
         ┌──────────────────────┐
         │ Try Block Starts     │
         │ (Error Handling)     │
         └──────────┬───────────┘
                    │
                    ▼
         ┌──────────────────────┐
         │ requests.get(url)    │
         │ Send HTTP GET Request│
         │ to OpenWeatherMap    │
         └──────────┬───────────┘
                    │
              ┌─────┴─────┐
              │           │
          Success      Error
              │           │
              ▼           ▼
        Response      Exception
        Received      Caught
              │           │
              ├───────┬───┘
              │       │
              ▼       ▼
         Continue  Exception
         Processing Handling
                    (See 3.1)
                    │
                    ▼
              display_error()
```

## 3.1 Exception Handling Flow

```
┌──────────────────────────────────────┐
│   Exception Caught in try-except     │
└──────────┬───────────────────────────┘
           │
    ┌──────┴──────┬──────────────────┐
    │             │                  │
    ▼             ▼                  ▼
┌────────────┐ ┌────────┐  ┌───────────────┐
│ HTTPError? │ │ConnErr?│  │TimeoutError?  │
└────────────┘ └────────┘  └───────────────┘
    │ YES         │ YES         │ YES
    │             │             │
    ▼             ▼             ▼
┌─────────────────────────────────────────┐
│ Check Status Code with match/case:      │
│                                         │
│ 400 → "Bad request"                     │
│ 401 → "Unauthorized"                    │
│ 403 → "Forbidden"                       │
│ 404 → "Not Found: City not found"       │
│ 500 → "Internal Server Error"           │
│ _ → Generic HTTP error                  │
└────────┬────────────────────────────────┘
         │
    ┌────┴────┬─────────────┬──────────────┐
    │          │             │              │
    ▼          ▼             ▼              ▼
 Call display_error() with appropriate error message
```

## 4. Weather Data Retrieval Flow

```
┌──────────────────────────────────────┐
│   response.raise_for_status()        │
│   Checks for HTTP errors             │
│   Raises HTTPError if 4xx or 5xx     │
└──────────┬───────────────────────────┘
           │
           ▼
┌──────────────────────────────────────┐
│   data = response.json()             │
│   Parse JSON response                │
└──────────┬───────────────────────────┘
           │
           ▼
┌──────────────────────────────────────┐
│   Check if data["cod"] == 200        │
│   (Success code)                     │
└──────────┬───────────────────────────┘
           │
      ┌────┴────┐
      │          │
    YES         NO
      │          │
      ▼          ▼
 Continue    Return Error
 Processing
      │
      ▼
┌──────────────────────────────────────┐
│   Call display_weather(data)         │
└──────────┬───────────────────────────┘
           │
           ▼
    ┌──────────────────┐
    │ display_weather()│
    └──────────────────┘
```

## 5. Display Weather Data Flow (display_weather Method)

```
┌──────────────────────────────────────┐
│   display_weather(data) Called       │
└──────────┬───────────────────────────┘
           │
    ┌──────┴──────────────┬────────────┐
    │                     │            │
    ▼                     ▼            ▼
┌──────────┐     ┌──────────────┐  ┌─────────┐
│ Extract  │     │ Extract      │  │ Extract │
│ Tempera- │     │ Weather ID   │  │Weather  │
│ ture     │     │              │  │Descrip- │
│ (Celsius)│     │ weather[0]   │  │tion     │
│          │     │ ["id"]       │  │         │
│ temp_c = │     │              │  │weather- │
│ data     │     │              │  │[0][desc│
│ ["main"] │     │              │  │ription]│
│ ["temp"] │     │              │  │        │
└─────┬────┘     └────┬─────────┘  └──┬─────┘
      │               │               │
      │               ▼               │
      │        ┌──────────────────┐   │
      │        │ Call             │   │
      │        │ get_weather_     │   │
      │        │ emoji(weather_id)│   │
      │        └────────┬─────────┘   │
      │                 │             │
      │                 ▼             │
      │        ┌──────────────────┐   │
      │        │ Returns Emoji    │   │
      │        │ String           │   │
      │        └────────┬─────────┘   │
      │                 │             │
      └─────────┬───────┴─────────────┘
                │
                ▼
    ┌───────────────────────────────┐
    │ Update Three Labels:          │
    │                               │
    │ 1. temperature_label.setText()│
    │    f"{temperature_c:.0f}°C"   │
    │    (No decimal places)        │
    │                               │
    │ 2. emoji_label.setText()      │
    │    (Weather emoji)            │
    │                               │
    │ 3. description_label.setText()│
    │    weather_description        │
    │    .capitalize()              │
    └──────────┬────────────────────┘
               │
               ▼
    Display Complete Weather Info
    to User
```

## 6. Weather Emoji Mapping Flow (get_weather_emoji Method)

```
┌────────────────────────────────────┐
│ get_weather_emoji(weather_id)      │
│ Static Method - Range Checking     │
└────────┬───────────────────────────┘
         │
    ┌────┴─────────────────────────────────┐
    │                                      │
    ▼                                      ▼
┌─────────────────────┐    ┌──────────────────────┐
│ Check ID Ranges     │    │ Check Specific IDs   │
│ (Thunderstorm,      │    │ and Conditions       │
│  Rain, Snow, etc.)  │    │                      │
└─────────────────────┘    └──────────────────────┘
    │                                      │
    │ Range Checks:                        │
    │ 200-232 → ⛈️ (Thunderstorm)         │
    │ 300-321 → 🌦️ (Drizzle)             │
    │ 500-531 → 🌧️ (Rain)                │
    │ 600-622 → ❄️ (Snow)                 │
    │ 701-741 → 🌫️ (Mist/Fog/Smoke)      │
    │ 801-804 → ☁️ (Clouds)               │
    │                                      │
    │ Specific IDs:                        │
    │ 762 → 🌋 (Volcanic Ash)             │
    │ 771 → 💨 (Squall)                   │
    │ 781 → 🌪️ (Tornado)                 │
    │ 800 → ☀️ (Clear Sky)                │
    │ else → "" (Empty String)            │
    │                                      │
    └──────────┬────────────────────────┘
               │
               ▼
    ┌────────────────────┐
    │ Return Emoji       │
    │ or Empty String    │
    └────────┬───────────┘
             │
             ▼
    Emoji Displayed in
    emoji_label
```

## 7. Error Display Flow (display_error Method)

```
┌──────────────────────────────────┐
│ display_error(message) Called    │
└──────────┬──────────────────────┘
           │
    ┌──────┴──────┬──────────────┐
    │             │              │
    ▼             ▼              ▼
┌────────────┐ ┌──────────┐  ┌──────────┐
│ Set Temp   │ │ Clear    │  │ Clear    │
│ Label Font │ │ Emoji    │  │ Descrip- │
│ Size to    │ │ Label    │  │ tion     │
│ 30px       │ │ (Remove  │  │ Label    │
│            │ │ Content) │  │          │
│ Shrink     │ │          │  │          │
│ from 75px  │ │          │  │          │
└────────────┘ └──────────┘  └──────────┘
    │             │              │
    └──────┬──────┴──────────────┘
           │
           ▼
┌──────────────────────────────────┐
│ temperature_label.setText()      │
│ Display Error Message Text       │
│                                  │
│ Examples:                        │
│ "Bad request:\nPlease check..." │
│ "Not Found:\nCity not found"    │
│ "Connection Error:\nCheck..."   │
└──────────┬───────────────────────┘
           │
           ▼
    Error Message Displayed
    to User with Large Font
```

## 8. Complete User Journey Flowchart

```
                    ┌─────────────────┐
                    │  Start App      │
                    └────────┬────────┘
                             │
                             ▼
                    ┌─────────────────┐
                    │ Show GUI Window │
                    │ Ready for Input │
                    └────────┬────────┘
                             │
                             ▼
                    ┌─────────────────┐
                    │ User Types City │
                    │ Name            │
                    └────────┬────────┘
                             │
                             ▼
                    ┌─────────────────┐
                    │ User Clicks     │
                    │ "Get Weather"   │
                    │ Button          │
                    └────────┬────────┘
                             │
                             ▼
                    ┌─────────────────┐
                    │ API Request     │
                    │ Sent            │
                    └────────┬────────┘
                             │
              ┌──────────────┴──────────────┐
              │                             │
              ▼                             ▼
         ┌─────────────┐           ┌────────────────┐
         │ API Success │           │ API Error or   │
         │ Response    │           │ Connection     │
         │ Received    │           │ Problem        │
         └──────┬──────┘           └────────┬───────┘
                │                          │
                ▼                          ▼
         ┌─────────────┐           ┌────────────────┐
         │ Parse JSON  │           │ Show Error     │
         │ Extract     │           │ Message in     │
         │ Data        │           │ Temperature    │
         └──────┬──────┘           │ Label          │
                │                  │                │
                ▼                  │ Clear Emoji    │
         ┌─────────────┐           │ & Description  │
         │ Display     │           │ Labels         │
         │ Temperature │           └────────┬───────┘
         │ Weather     │                    │
         │ Emoji       │                    ▼
         │ Description │           ┌────────────────┐
         └──────┬──────┘           │ User Sees      │
                │                  │ Error Message  │
                ▼                  └────────┬───────┘
         ┌─────────────┐                    │
         │ User Sees   │                    │
         │ Complete    │                    │
         │ Weather     │                    │
         │ Information │                    │
         └──────┬──────┘                    │
                │                          │
                └──────────────┬───────────┘
                               │
                               ▼
                    ┌─────────────────────┐
                    │ User Can Search     │
                    │ Another City or     │
                    │ Exit Application    │
                    └─────────────────────┘
```

## 9. API Request and Response Timeline

```
TIME    ACTION                          DESCRIPTION
────────────────────────────────────────────────────────────────
T0      User Input                      City name entered in field
        ↓
T1      Button Click                    "Get Weather" button clicked
        ↓
T2      Extract Input                   city_input.text() retrieved
        ↓
T3      Build URL                       URL constructed with parameters
        ↓
T4      HTTP Request Sent               requests.get(url) called
        ↓
T5-T6   Network Latency                 Request traveling to API server
        ↓                               (typically 500ms-2000ms)
T7      API Processing                  OpenWeatherMap server processes
        ↓                               request
T8-T9   Network Latency                 Response traveling back to app
        ↓                               (typically 500ms-2000ms)
T10     Response Received               response object returned
        ↓
T11     Status Check                    raise_for_status() checks code
        ↓
T12     JSON Parse                      response.json() extracts data
        ↓
T13     Data Validation                 Check cod == 200
        ↓
T14     Display Update                  Update temperature, emoji,
        ↓                               description labels
T15     User Sees Result                Weather data visible on screen
```

## 10. Data Structure - API Response Example

```
OpenWeatherMap API Response (JSON):
┌─────────────────────────────────────┐
│ {                                   │
│   "cod": "200",              ←──────┼─── Status Code (String)
│   "main": {                  ←──────┼─── Main Data Object
│     "temp": 15.5             ←──────┼─── Temperature (Celsius)
│   },                          │      │
│   "weather": [               ←──────┼─── Weather Array
│     {                         │      │
│       "id": 800              ←──────┼─── Weather ID (Used for emoji)
│       "description": "clear sky"    │
│     }                        ←──────┼─── Description String
│   ]                           │      │
│ }                             │      │
└─────────────────────────────────────┘
```

## 11. Widget Hierarchy and Layout

```
QWidget (WeatherApp)
│
└─ QVBoxLayout
   │
   ├─ QLabel: city_label
   │  ├─ Font: Calibri, 40px, italic
   │  ├─ Alignment: Center
   │  └─ Text: "Enter city name: "
   │
   ├─ QLineEdit: city_input
   │  ├─ Font: Calibri, 40px
   │  ├─ Alignment: Center
   │  └─ Placeholder: (for user text input)
   │
   ├─ QPushButton: get_weather_button
   │  ├─ Font: Calibri, 30px, bold
   │  ├─ Text: "Get Weather"
   │  └─ Signal: clicked → get_weather()
   │
   ├─ QLabel: temperature_label
   │  ├─ Font: 75px (Normal) or 30px (Error)
   │  ├─ Alignment: Center
   │  └─ Content: Temperature or Error Message
   │
   ├─ QLabel: emoji_label
   │  ├─ Font: 100px, Segoe UI emoji
   │  ├─ Alignment: Center
   │  └─ Content: Weather emoji (⛈️ ☀️ 🌧️ etc.)
   │
   └─ QLabel: description_label
      ├─ Font: Calibri, 50px
      ├─ Alignment: Center
      └─ Content: Weather description (capitalized)
```

## 12. Exception Handling Decision Tree

```
                    Exception Occurs
                           │
                    ┌──────┴──────┐
                    │             │
                    ▼             ▼
            ┌──────────────┐  ┌─────────────────────┐
            │ HTTPError?   │  │ ConnectionError?    │
            └──┬───────┬──┘  └────────┬────────┬───┘
               │       │              │        │
              YES     NO             YES       NO
               │       │              │        │
               ▼       ▼              ▼        ▼
        ┌────────────┐         Display:    ┌──────────────┐
        │ Match      │         "Connection"│ TimeoutError?│
        │ Status     │         Error:      └────┬───────┬─┘
        │ Code       │         Check your  │    │       │
        └────────────┘         internet    YES  │       NO
            │                  connection  │    │       │
    ┌───────┼───────┐                      ▼    │       ▼
    │       │       │              Display │   │  ┌──────────┐
    ▼       ▼       ▼               "Timeout"  │  │RequestExc│
  400    404   500+                Error:      │  │eption?   │
   │      │      │                 Try again  │  └─────┬────┘
   ▼      ▼      ▼                 later      │        │
"Bad"  "Not"  "Server"                       YES       │
"Req"  "Found" "Error"                         │       NO
   │      │      │                            ▼       │
   └──────┴──────┘                       Display:    │
        │                               "Request    ▼
        ▼                               Error"   Generic
   display_error()                          │    Exception
   called with msg                         ▼     │
        │                            display_  ▼
        ▼                            error()  Display
   Error shown                                Generic
   to user                                   Error Msg
```

## 13. Method Call Sequence Diagram

```
┌─────────┐  ┌──────────────┐  ┌──────────┐  ┌────────┐  ┌─────────────┐
│  User   │  │ WeatherApp   │  │  PyQt5   │  │Requests│  │OpenWeatherM │
│(GUI)    │  │(Main Class)  │  │(Framework)   │Library │  │ap API       │
└────┬────┘  └──────┬───────┘  └────┬─────┘  └───┬────┘  └──────┬──────┘
     │              │               │            │             │
     │              │               │            │             │
 Clicks│             │               │            │             │
 Button├─────────────>               │            │             │
 "Get  │  get_weather()              │            │             │
Weather│             │               │            │             │
 "     │             ├──────────────>get_weather  │             │
     │             │       Button   │            │             │
     │             │       Click    │            │             │
     │             │               │            │             │
     │             ├─────────────────────────────>make API call │
     │             │        requests.get()       │             │
     │             │               │            │             │
     │             │               │            ├────────────>send HTTP
     │             │               │            │  GET request │
     │             │               │            │             │
     │             │               │            │             │<─query
     │             │               │            │    process   │
     │             │               │            │             │
     │             │               │            │<────────JSON─┤
     │             │               │            │ response    │
     │             │<─────────────response object             │
     │             │       requests library                    │
     │             │               │                          │
     │             ├─check status code                         │
     │             ├─parse JSON                                │
     │             ├─get_weather_emoji()                       │
     │             │(static method)                            │
     │             │               │                          │
     │             ├─display_weather()                         │
     │             │               │                          │
     │             ├─update labels                             │
     │             │               │                          │
     │<────────────┤               │                          │
  Sees │ display complete result  │                          │
 Data  │               │           │                          │
 on    │               │           │                          │
Screen │               │           │                          │
     │               │           │                          │
```

## 14. CSS Styling Hierarchy

```
┌──────────────────────────────────────────────────┐
│          CSS Stylesheet Applied                  │
└──────────────────────────────────────────────────┘
                        │
        ┌───────────────┼───────────────┐
        │               │               │
        ▼               ▼               ▼
┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│ General      │ │ QLabel Rules │ │ QPushButton  │
│ Rules        │ │              │ │ Rules        │
│              │ │ All QLabels: │ │              │
│ QLabel,      │ │ font-family: │ │ #button:     │
│ QPushButton: │ │ calibri      │ │ font-size:   │
│ font-family: │ │              │ │ 30px         │
│ calibri      │ │ Specific:    │ │ font-weight: │
└──────────────┘ │ #city_label  │ │ bold         │
        │        │   40px italic│ └──────────────┘
        │        │              │        │
        │        │ #temperature │        │
        │        │   75px       │        │
        │        │              │        │
        │        │ #emoji       │        │
        │        │   100px      │        │
        │        │   Segoe UI   │        │
        │        │   emoji      │        │
        │        │              │        │
        │        │ #description │        │
        │        │   50px       │        │
        │        └──────────────┘        │
        │               │                │
        │               │                │
        └───────────────┼────────────────┘
                        │
                        ▼
        ┌─────────────────────────────┐
        │ Widgets Rendered with       │
        │ Complete Styling Applied    │
        │ Fonts, Sizes, Colors,       │
        │ and Alignment               │
        └─────────────────────────────┘
```

## 15. Thread and Performance Considerations

```
EXECUTION MODEL (SYNCHRONOUS - Current Implementation)
──────────────────────────────────────────────────────

User Interaction Timeline:

T0:     [User] Clicks Button
        │
        ├─────> [PyQt5 Event Handler] Fires
                │
                ├─────> [get_weather()] Executes
                        │
                        ├─────> [requests.get()] Blocks Here
                        │       (Network I/O - 1-3 seconds)
                        │       UI IS FROZEN DURING THIS TIME
                        │
                        ├─────> Response Received
                        │
                        ├─────> [display_weather()] Updates
                        │
T3:     [PyQt5 Event Loop] Resumes
        │
        └─────> [UI] Becomes responsive again


PERFORMANCE CHARACTERISTICS:
──────────────────────────────

Response Time Breakdown:
├─ User input to API call:    ~100ms
├─ Network latency (request): ~500-1500ms
├─ API server processing:     ~200-500ms
├─ Network latency (response):~500-1500ms
└─ UI update rendering:       ~50-100ms
  ────────────────────────────────────
  TOTAL:                       ~1.5-3.5 seconds

During API call:
- UI is BLOCKED (frozen)
- User cannot interact with window
- No responsiveness

Suggested Improvement (For Production):
Use threading or QThread to move API calls
off the main thread:

def get_weather_threaded(self):
    thread = threading.Thread(target=self.get_weather)
    thread.daemon = True
    thread.start()

This would:
✓ Keep UI responsive
✓ Allow user to click again
✓ Prevent "Not Responding" errors
```

## 16. Weather ID Classification System

```
WEATHER CONDITION CATEGORIES:
────────────────────────────────────────────

┌─────────────┬─────────────────┬────────┐
│ ID Range    │ Condition       │ Emoji  │
├─────────────┼─────────────────┼────────┤
│ 200-232     │ Thunderstorm    │ ⛈️    │
│             │ (Severe)        │        │
├─────────────┼─────────────────┼────────┤
│ 300-321     │ Drizzle         │ 🌦️    │
│             │ (Light Rain)    │        │
├─────────────┼─────────────────┼────────┤
│ 500-531     │ Rain            │ 🌧️    │
│             │ (Moderate/Heavy)│        │
├─────────────┼─────────────────┼────────┤
│ 600-622     │ Snow            │ ❄️    │
│             │ (Various types) │        │
├─────────────┼─────────────────┼────────┤
│ 701-741     │ Atmosphere      │ 🌫️    │
│             │ (Mist/Fog)      │        │
├─────────────┼─────────────────┼────────┤
│ 762         │ Volcanic Ash    │ 🌋    │
├─────────────┼─────────────────┼────────┤
│ 771         │ Squall          │ 💨    │
├─────────────┼─────────────────┼────────┤
│ 781         │ Tornado         │ 🌪️    │
├─────────────┼─────────────────┼────────┤
│ 800         │ Clear Sky       │ ☀️    │
│             │ (Sunny)         │        │
├─────────────┼─────────────────┼────────┤
│ 801-804     │ Clouds          │ ☁️    │
│             │ (Partly/Mostly) │        │
├─────────────┼─────────────────┼────────┤
│ else        │ Unknown         │ (none) │
└─────────────┴─────────────────┴────────┘

COVERAGE: Covers all standard weather conditions
from OpenWeatherMap API classification system.
```

## 17. Error Message Mapping

```
HTTP ERRORS:
─────────────────────────────────────────

Status Code     Error Type          Display Message
──────────────────────────────────────────────────
400             Bad Request         "Bad request:\nPlease check your input"
401             Unauthorized        "Unauthorized:\nInvalid API key"
403             Forbidden           "Forbidden:\nAccess is denied"
404             Not Found           "Not Found:\nCity not found"
500             Server Error        "Internal Server Error:\nPlease try later"
Other 4xx/5xx   Generic Error       f"HTTP error:\n{http_error}"

CONNECTION ERRORS:
──────────────────────────────────────────

Exception Type              Display Message
──────────────────────────────────────────────────
ConnectionError             "Connection Error:\nCheck your internet"
TimeoutError                "Timeout Error:\nThe request timed out"
RequestException (generic)  f"Request Error:\n{req_error}"

COMMON CAUSES:
──────────────────────────────────────────

Error                    Cause
──────────────────────────────────────────────
"City not found"        - Misspelled city name
                        - City doesn't exist in database

"Connection Error"      - No internet
                        - Firewall blocking request
                        - ISP blocking API

"Timeout Error"         - Network very slow
                        - API server overloaded
                        - Network congestion

"Internal Server Error" - OpenWeatherMap API down
                        - Maintenance in progress
```

## 18. Data Flow Diagram - Complete Journey

```
┌─────────────────────────────────────────────┐
│            START APPLICATION                │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│         DISPLAY GUI WINDOW                  │
│    ├─ City Label: "Enter city name:"        │
│    ├─ City Input: (empty text field)        │
│    ├─ Get Weather Button: "Get Weather"     │
│    ├─ Temperature Label: (empty)            │
│    ├─ Emoji Label: (empty)                  │
│    └─ Description Label: (empty)            │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│      WAIT FOR USER INTERACTION              │
│      (Event Loop Running)                   │
└──────────────────┬──────────────────────────┘
                   │
          ┌────────┴────────┐
          │                 │
    USER INPUT        USER ACTION
    (Type City)       (Click Button)
          │                 │
          └────────┬────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│    EXTRACT DATA FROM UI FIELD               │
│    city = self.city_input.text()            │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│    BUILD API REQUEST URL                    │
│    Include: city, API key, units (metric)   │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│    SEND HTTP GET REQUEST                    │
│    To: api.openweathermap.org               │
└──────────────────┬──────────────────────────┘
                   │
          ┌────────┴────────┐
          │                 │
       SUCCESS           FAILURE
          │                 │
          ▼                 ▼
┌──────────────┐    ┌─────────────────┐
│ Receive      │    │ Catch Exception │
│ JSON         │    │ - HTTPError     │
│ Response     │    │ - ConnError     │
└──────┬───────┘    │ - TimeoutError  │
       │            │ - RequestError  │
       ▼            └────────┬────────┘
┌──────────────┐             │
│ Parse JSON   │             ▼
└──────┬───────┘    ┌──────────────┐
       │            │ Format Error  │
       ▼            │ Message       │
┌──────────────┐    └──────┬───────┘
│ Validate     │           │
│ cod == 200   │           ▼
└──────┬───────┘    ┌──────────────┐
       │            │ Call          │
       │            │ display_error │
       │            └──────┬───────┘
       │                   │
       │                   ▼
       │            ┌──────────────┐
       │            │ Show Error   │
       │            │ Message in   │
       │            │ Temp Label   │
       │            │ (30px font)  │
       │            │              │
       │            │ Clear Emoji  │
       │            │ & Description│
       │            └──────┬───────┘
       │                   │
       ├───────────────────┘
       │
       ▼
┌──────────────┐
│ Extract Data │
│ - temp       │
│ - weather_id │
│ - description│
└──────┬───────┘
       │
       ▼
┌──────────────┐
│ Get Emoji    │
│ for weather_ │
│ id           │
└──────┬───────┘
       │
       ▼
┌──────────────┐
│ Call         │
│ display_     │
│ weather()    │
└──────┬───────┘
       │
       ▼
┌──────────────────────────────┐
│ Update All Three Labels:     │
│                              │
│ 1. Temperature Label:        │
│    "{temp:.0f}°C" (75px)    │
│                              │
│ 2. Emoji Label:              │
│    Display emoji (100px)     │
│                              │
│ 3. Description Label:        │
│    Display text (50px)       │
└──────────────┬───────────────┘
               │
               ▼
┌──────────────────────────────┐
│ WEATHER INFORMATION DISPLAYED│
│ TO USER                      │
│                              │
│ Example:                     │
│ Temp: 15°C                   │
│ Emoji: ☀️                   │
│ Description: Clear sky       │
└──────────────┬───────────────┘
               │
               ▼
┌──────────────────────────────┐
│ USER CAN:                    │
│ • Search another city        │
│ • Clear input field          │
│ • Exit application           │
│ • Close window               │
└──────────────────────────────┘
```

## 19. Static Method Behavior

```
STATIC METHOD: get_weather_emoji()
───────────────────────────────────

Characteristics:
- Belongs to class, not instance
- Called as: WeatherApp.get_weather_emoji(id)
- Or: self.get_weather_emoji(id)
- Does NOT use self parameter
- Pure function - no side effects

Implementation:
@staticmethod
def get_weather_emoji(weather_id):
    # Ranges and conditionals only
    # No instance state accessed
    # Only returns value

Benefits:
✓ Can be called without creating instance
✓ Testable independently
✓ Reusable utility function
✓ No unnecessary object overhead
✓ Clear intent - pure function

Usage in this app:
- Called from display_weather()
- Maps numeric weather code to emoji
- Deterministic (same input = same output)
- No external dependencies
```

## 20. Key Implementation Features Summary

```
┌────────────────────────────────────────────┐
│          FEATURE BREAKDOWN                 │
└────────────────────────────────────────────┘

FEATURE              IMPLEMENTATION METHOD
──────────────────────────────────────────────

GUI Framework        PyQt5 QWidget-based
User Input           QLineEdit text capture
Button Action        Signal-slot connection
Data Display         QLabel setText()
Network Requests     requests library
JSON Parsing         response.json()
Error Handling       try-except-else
                     match-case statements
HTTP Status Check    raise_for_status()
Weather Emoji        Static method mapping
CSS Styling          setStyleSheet()
Layout Management    QVBoxLayout
Font Customization   Qt stylesheets
String Formatting    f-strings
Temperature Format   .0f (zero decimals)
Description Format   .capitalize()
Exception Types      6 different types

DESIGN PATTERNS:
- Object-Oriented (OOP)
- Signal-Slot (Qt)
- Static Method
- Try-Catch Pattern
- Pattern Matching (match-case)

TECHNOLOGIES:
- Python 3.x
- PyQt5
- HTTP (requests library)
- REST API
- JSON format
- CSS (for styling)
```

## 21. Environmental Setup Diagram

```
DEPLOYMENT REQUIREMENTS:
────────────────────────

┌──────────────────────────────────────┐
│     SYSTEM REQUIREMENTS              │
├──────────────────────────────────────┤
│ • Python 3.7+                        │
│ • pip (package manager)              │
│ • Internet connection                │
│ • OpenWeatherMap API key             │
└──────────────────────────────────────┘

┌──────────────────────────────────────┐
│     DEPENDENCIES TO INSTALL          │
├──────────────────────────────────────┤
│ pip install PyQt5                    │
│ pip install requests                 │
└──────────────────────────────────────┘

┌──────────────────────────────────────┐
│     ENVIRONMENT VARIABLES            │
├──────────────────────────────────────┤
│ (Optional - for security)            │
│ OPENWEATHER_API_KEY=<your_key>      │
└──────────────────────────────────────┘

┌──────────────────────────────────────┐
│     EXECUTION COMMAND                │
├──────────────────────────────────────┤
│ python weather_app.py                │
│ (from project directory)             │
└──────────────────────────────────────┘

┌──────────────────────────────────────┐
│     EXPECTED OUTPUT                  │
├──────────────────────────────────────┤
│ • GUI window appears                 │
│ • Ready for city input               │
│ • Responsive to user interaction     │
└──────────────────────────────────────┘
```

---

## Summary

The Weather App Flowchart documentation provides:

1. **Application Initialization**: Complete startup sequence from program launch to GUI display
2. **UI Setup**: Detailed widget hierarchy and styling process
3. **User Interaction Flow**: Event-driven programming with button clicks
4. **API Integration**: HTTP request-response cycle with OpenWeatherMap
5. **Error Handling**: Comprehensive exception management with user-friendly messages
6. **Data Processing**: JSON parsing and weather data extraction
7. **Display Updates**: Real-time UI label updates with temperature, emoji, and description
8. **Weather Emoji Mapping**: Static method for converting weather codes to emoji
9. **Performance Characteristics**: Synchronous execution model with blocking network calls
10. **Complete Journey**: End-to-end user experience from input to result display

The implementation demonstrates practical GUI development with API integration, robust error handling, and professional styling in PyQt5.
